// Visible slot window shared by CalendarGrid and the now-line position math —
// single source of truth so the two never drift out of sync.
export const SLOT_START_HOUR = 7
export const SLOT_COUNT = 28 // 07:00–20:30 in 30-min steps
